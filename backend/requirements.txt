fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-multipart>=0.0.9
# milvus-lite 依赖 pkg_resources，setuptools>=71 移除了它，必须锁定旧版
setuptools>=65.0.0,<71

qwen-agent>=0.0.7
dashscope>=1.14.0
pandas>=2.0.0
sqlalchemy>=2.0.0
mysql-connector-python>=8.3.0
python-dotenv>=1.0.0
soundfile>=0.12.0
tabulate>=0.9.0

# 文档助手 —— LlamaIndex 数据层
llama-index-core>=0.10.0
llama-index-llms-dashscope>=0.1.0
llama-index-embeddings-dashscope>=0.1.0
llama-index-readers-file>=0.1.0
# Milvus 向量存储
# 通过 MILVUS_URI 环境变量切换模式：
#   - 本地开发: 给一个文件路径走 Milvus Lite (需 pip install pymilvus[milvus_lite])
#   - 生产部署: 给一个 http://host:19530 端点走 Milvus Standalone / Cluster / Zilliz Cloud
# 默认依赖只装客户端，不装 milvus-lite，避免在生产 Linux 上的 wheel 兼容性问题
llama-index-vector-stores-milvus>=0.1.20
pymilvus>=2.4.2

# 文档助手 —— LangChain 编排层
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0

# 文档解析
pypdf>=4.0.0
python-docx>=1.1.0
PyMuPDF>=1.24.0
rapidocr-onnxruntime>=1.2.0
Pillow>=10.0.0

# 股票助手
tushare>=1.4.0
pymysql>=1.1.0
apscheduler>=3.10.0,<4.0
